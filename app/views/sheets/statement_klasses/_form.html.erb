<%= form_with(model: sheets_statement_klass, local: true) do |form| %>
  <% if sheets_statement_klass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sheets_statement_klass.errors.count, "error") %> prohibited this sheets_statement_klass from being saved:</h2>

      <ul>
        <% sheets_statement_klass.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lang_key %>
    <%= form.text_field :lang_key %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
